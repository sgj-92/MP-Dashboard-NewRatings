// ===================== BUILD INFO (local placeholder) =====================
// This is NOT the file a deployed device runs.
//
// GitHub Pages builds the site with Jekyll on every push to main, and Jekyll
// renders assets/js/buildInfo.pages.js to this path with the commit being
// built. _config.yml excludes this placeholder so it can never overwrite the
// stamped copy. Locally (tests, screenshots, opening index.html from disk)
// there is no build, so the More screen says so rather than inventing one.
//
// Never write a SHA or a date in here by hand: the point of the stamp is that
// nobody maintains it.
window.MP_BUILD = { sha: null, date: null };
