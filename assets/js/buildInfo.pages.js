---
# Rendered by GitHub Pages' Jekyll build, never loaded as-is. Output replaces
# the local placeholder at assets/js/buildInfo.js (see _config.yml).
# site.github.build_revision is the commit Pages is building -- the same SHA
# the "pages build and deployment" run reports -- and site.time is when that
# build ran, in the timezone _config.yml sets.
permalink: /assets/js/buildInfo.js
layout: null
sitemap: false
---
window.MP_BUILD = { sha: "{{ site.github.build_revision }}", date: "{{ site.time | date: '%Y-%m-%d' }}" };
